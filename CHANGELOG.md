# Changelog

このプロジェクトの主だった変更をここに残す。

書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/)、バージョンは
[Semantic Versioning](https://semver.org/lang/ja/) に従う。1.0 より前は、破壊的変更でマイナーを上げる。

## [Unreleased]

### Added

- 出荷する値の pin（`rosheet.lock.json`）。`rosheet tag <name>` で commit に名前を付け、`rosheet update --tag <name>` でそこへ pin を移す。`pull` / `check` は pin を焼くので、**誰が Apply しても出荷値は動かない** —— 動かすには lock の diff、つまり PR が要る。pin が無ければ今までどおり head を焼くので、既存のプロジェクトの挙動は `update` を打つまで変わらない。
- lock はハッシュではなく**スキーマと値ごと**持つ。rosheet を上げて生成物の書式が変わっても `check` が正しく回り、副産物として `pull` / `check` / `import --local` / `export --local` が Open Cloud のキー無しで回る。キーを持たない貢献者・CI・ブランチ限定の値・空の DataStore の初期化が、これで CLI だけで閉じる。DataStore を読むのは `update` / `status` / `log` / `revert` / `import` だけになった。
- CLI に `update` / `status` / `tag` を足した。`status` は pin と head の差、lock を手で直したシート、生成物のずれを並べる。
- 生成物のヘッダに出所を焼く（`-- Generated by rosheet from sheet "items" at commit #42 (v0.3.0).`）。pull した時刻は焼かない —— 焼くと同じデータを引き直すたびに差分が出て `check` が誤検知する。**既存の生成物とは 1 行違うので、上げたあと最初の `check` は落ちる。`pull` し直してコミットする。**
- `writablePlaces`。`defineSchema` の第 2 引数で書いてよい place を宣言すると、そこに無い place ではプラグインが読み取り専用になる（表と履歴は読めるが、Apply もスキーマの publish もしない）。同じ universe の place は DataStore を共有するので、本番 place を Studio で開くこと自体が書き込み権限になっていた。**事故を止めるためのもので、権限の境界ではない。**
- プラグインのステータス行に、head が一番新しい tag より何 commit 先かを出す（`#45 · 3 ahead of v0.3.0`）。編集する側が「自分の変更はまだ出荷されていない」と分かる。止めるためのものではない。

### Fixed

- スキーマの Source が変わっても自動で読み直さなかった。`_placeChanged` がスキーマ未読み込みのときしか `reload()` を呼んでおらず、届いた変更をそこで捨てていた（開き直せば反映される、という症状）。引き金に理由を持たせ、スキーマの変化だけで読み直す。木の増減では読み直さない —— `rbxtsc` のビルド 1 回で `out/` の木ごと入れ替わるので、読み直すとビルドのたびに DataStore へ書くことになる。編集中の下書きがあるときは黙って読み直さず、変わったことだけ出す（Apply していない編集は履歴にも無いので戻せない）。
- スキーマの publish が無条件だった。place が変わるたびに走るので、中身が同じでも書き直していた。実際に置けたものと比べて、変わったときだけ書く。
- 読み取り専用の place でも、行を足せてセルに文字も打てた。止まるのは Apply だけだったので、**保存できない下書きをいくらでも作れ**、気づくのは Apply に手を伸ばしたときだった。表からの編集と行の増減も止める。セルは `readonly` 列と同じ見た目（灰色の文字と背景、候補の ▾ も出さない）に寄せた。
- `rosheet runtime` でランタイムを上げても、プラグインが古い `defineSchema` を評価し続け、新しい宣言（今回は `writablePlaces`）がエラーも警告も無しに落ちていた。`requireFresh` は外側のスキーマモジュールしか写せず、その中の `require(script.Parent.rosheet.schema)` は元のインスタンスを指すので、ランタイムの恒久キャッシュには当たったままになる。内側の require まで写す手が無いので、**見つけて言う** —— ランタイムに版を持たせ、place に置かれた Source の版と、require が返す版（＝キャッシュに載っている方）を突き合わせる。ズレているあいだは読み取り専用にして、Apply もスキーマの publish も止める（落ちた宣言に `writablePlaces` が含まれていると、本番 place を守るはずの宣言が消えたまま書けてしまうため）。`rosheet runtime` も、書いたときは Studio を開き直すよう出す。
- `defineSchema` の第 2 引数が、知らない鍵を黙って捨てていた。落とすようにした。**`{ writeablePlaces = ... }` のような書き間違いは、これまで何も言わずに無視されていた。**
- `scope` を設定すると、Studio プラグインと CLI が別のキースペースを見ていた。CLI は `rosheet.toml` から読むだけで、Open Cloud の URL に載せていなかった（プラグインは `GetDataStore(name, scope)` で使う）。設定した側からは「プラグインでは編集できているのに pull が何も見つけない」としか見えなかった。空の `scope` は設定の誤りとして落とす。

## [0.1.0] - 2026-09-09

### Added

- **ダブルクリックで入るプラグインのインストーラーを、各 Release に添付する。** プラグインを入れるのに Node と npm を入れて `rosheet plugin` を叩く必要があり、値を触るだけの人に求めるには重かった。`scripts/build-installers.js` が `rosheet-plugin-installer-windows.cmd` と `rosheet-plugin-installer-macos.zip` を組み立て、リリースの workflow が添付する。プラグインには何も焼き込まないので、`.rbxmx` を base64 で各インストーラーの中に抱えられる。置き先とファイル名は `rosheet plugin` と同じで、インストール時に通信しない。macOS 版を zip で包むのは、Release のアセットにパーミッションが残らず、実行ビットの落ちた `.command` はダブルクリックできないため。`rosheet.rbxmx` 単体も、手で置きたい人向けに添付する。
- スキーマをコードで定義する（Luau / roblox-ts）。列の型・readonly・既定値・enum の候補・数値の範囲を宣言し、Luau 側と JS 側で同じ正規形に落ちることをテストで検査している。
- Studio プラグイン。宣言したシートごとに表ビューを出し、編集できる列だけ編集させ、Apply でプレイテスト中の値を差し替える。設定もスキーマもアセット一覧も焼き込まず、place の中から読む。
- DataStore を編集の正とする履歴。1 回の Apply が 1 つの commit として積まれ、`log` で一覧、`revert` で任意の時点へ戻す commit を積める。
- CLI: `init` / `plugin` / `runtime` / `pull` / `check` / `log` / `revert` / `export` / `import` / `schema`。`check` は生成物が現在値とずれていれば落ちるので、Apply の取り込み忘れを CI で捕まえられる。
- `rosheet runtime`。`runtime/` を利用側のプロジェクトへ複製する。roblox-ts のプロジェクトからは `node_modules` の中の `.d.ts` へ型として届かない（npm スコープを typeRoots に足すと、`types` を持たないパッケージが暗黙の型ライブラリとして落ちる）ので、rbxtsc と Rojo が元から見ている木の中へ置く。
- 型付きのコード生成。`*.luau`（`--!strict`）と、roblox-ts 向けの `*.luau` + `*.d.ts`。
- CSV の出入り（`export` / `import`）。スプレッドシートや外部ツールとの退路で、CSV は正ではない。
- ランタイムの `bind`。Apply を反映させたいところだけ live 反映を通し、本番では同梱値をそのまま返す。
- `runtime/init.luau`。`schema` と `live` をまとめて返すので、`require(ReplicatedStorage.Packages.rosheet)` 1 本から `defineSchema` も `bind` も取れる（roblox-ts 向けの型は `runtime/index.d.ts`）。
- `asset` 列の rocas 連携。`rocas://assets/...` を書くと Studio で補完とサムネイルが出て、`pull` のときに `rbxassetid://` へ解決される。

### Fixed

- スキーマを直して Rojo が同期しても、プラグインが古いスキーマのままだった。`require` はモジュールの戻り値をインスタンス単位で恒久的にキャッシュし、Rojo は同じインスタンスの Source を書き換えるだけなので、Reload を押しても直らなかった。読むたびに写しを作って require する。
- スキーマが新しくなっても、表のセルが作り直されていなかった。シート名しか見ていなかったので、`readonly` を足しても TextBox のままだった。列の名前・型・readonly・既定値・enum の候補まで含めて見分ける。
- キー列が `readonly` で `rows = "open"` の表で、足した行の key を直せなかった（`new1` のまま確定して消すしかなかった）。まだ Apply していない行に限って、キー列を書き換えられるようにした。空と重複はその場で弾く。

### Note

- pre-alpha。**Studio の実機での往復（実 DataStore との疎通、Apply からプレイテストへの反映、rocas のアセット読み取り）はまだ確認していない。** 確かめた範囲は [README の進捗](README.md#進捗) を参照。
